---
export interface Props {
  src: string;
  alt: string;
  class?: string;
}

const { src, alt, class: className = '' } = Astro.props;
---

<img src={src} alt={alt} class={className} style="width: 100%; height: 300px; object-fit: contain;" />
